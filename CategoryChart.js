import React from 'react';

const CategoryChart = ({ chartData }) => {
  return (
    <div>
      <h1>sdfsdsfdfd</h1>
      <img src={`data:image/png;base64,${chartData}`} alt="Category Chart" />
    </div>
  );
};

export default CategoryChart;
